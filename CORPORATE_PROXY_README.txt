CORPORATE PROXY READY

Proxy: http://192.168.227.254:3128

On work computer run:
  run-corporate-proxy-dev.cmd

For desktop test:
  run-corporate-proxy-desktop.cmd

For EXE build:
  build-exe-corporate-proxy.cmd

If the proxy address changes, edit MAP_PROXY_URL inside the .cmd files.
